# default crypto profiles

this skillet loads the default crypto profiles into a PAN-OS NGFW.

typically part of the default configuration, here as required in the
event the default are lost or corrupted.